#pragma once
#include <iostream>

template<typename AnyType>
class Node
{

public:

	/// <summary>
	/// A node is part of a Linked List. It holds a source of data, the next node in the List, and it's previous node in the List
	/// </summary>
	/// <param name="next">The node directly after this instance of a node in the linked list</param>
	/// <param name="previous">The node directly before this instance of a node in the linked list</param>
	/// <param name="dataT">the data/information that this intance of a node holds within it.</param>
	Node(Node next, Node previous, AnyType dataT)
	{
		
	}


private:




};
	







